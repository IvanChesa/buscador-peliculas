# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Variables de entorno (NUNCA subir las claves al repositorio).
# Ojo: `*.local` de arriba ya cubre .env.local, pero lo dejamos explícito
# para que se vea la intención y por si algún día se usa otro nombre.
.env
.env.*
!.env.example
